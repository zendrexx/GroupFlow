# Models

Data Transfer Objects (DTOs).

Responsibilities:
- Parse JSON.
- Convert JSON to objects.
- Convert objects to entities.

Example:
- TaskModel

Rules:
- Used only in the Data layer.
- Can contain fromJson() and toJson().